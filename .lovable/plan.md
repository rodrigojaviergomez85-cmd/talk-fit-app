Actualizar instrucciones del paso 3 (Slow shadowing)

Cambiar el texto de instrucciones del paso 3 en `src/routes/practice.tsx`:

- Inglés: de `"Speak WITH the model."` a `"Read along with the model"`.
- Español: de `"Habla CON el modelo."` a `"Lee a la par del modelo"`.

El heading "Slow shadowing" y las notas de ritmo/velocidad permanecen iguales. No se modifica ninguna otra lógica, componente o paso.

Verificar typecheck y ruta `/practice` tras el cambio.
